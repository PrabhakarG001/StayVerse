<% if (hotels && hotels.length > 0) { %>
  <% hotels.slice(0, 7).forEach(hotel => { %>
    <%- include('./hotel-card', { hotel }) %>
  <% }) %>
  <%- include('./see-all-card', { city, hotels }) %>
<% } else { %>
  <p class="text-secondary">No homes found.</p>
<% } %>
